services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Screenings, stage decisions and audit runs survive a rebuild.
      - screening-data:/app/data
    restart: unless-stopped

volumes:
  screening-data:
